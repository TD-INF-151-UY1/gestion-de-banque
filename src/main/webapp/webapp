<!DOCTYPE html>
<html lang="fr">
<head> 
  <meta charset="UTF-8">
  <meta name="viewport"
content="width=device-width, initial-scale=1.0">
  <title> Logiciel Bancaire - Interface Utilisateur</title>
  <link rel="stylesheet"href="banking_style.css">
</head>
<body>
    <header>
    <h1>Logiciel Bancaire</h1>
        <nav>
          <a href="#dashboard"class="nav-link active">Tableau de Bord</a>
          <a href="#transaction"class="nav-link">Transaction</a>
          <a href="#create-account"class="nav-link">Creéer un compte</a>
          <a href="#Security"class="nav-link">Securité</a>
        </nav>
    </header>
   <main>
      <section id="dashboard" class="page active">
      <h2>Vue d'ensemble du Compte</h2>
         <div id="balance-info">
             <p>Type de Compte:</p>
             <strong>Compte Courant</strong>
         <p>Numéro de Compte: <strong>123456789</strong></p> Veuillez patienter....<p>solde actuel:<span id="currentbalance" class="balance-amount">veuiller patienter.....</span> </p>
         <button class="action-button
                        primary-button"
                 onclick="displayBalance('123456789')">Actualiser le Solde</button>
         </div>
        <h3>Historique des transactions</h3>
        <div id="transaction-history"
             class="data-table">
          <table>
            <thead>
                <tr><th>Date</th><th>Type</th><th>Montant</th></tr>
            </thead>
              <tbody>
                  <tr><td>2025-12-15</td><td>Dépot</td><td>1000.00 FCFA</td></tr>
                  <tr><td>2025-12-16</td><td>Retrait</td><td>-500.00 FCFA</td></tr>
              </tbody>
            </table>
        </div>
      </section>
      <section id="transaction"class="page">
          <h2>Effectuer une Transaction</h2>
          <form id="transactionForm"class="standard-form">
           <div class="form-group">
               <label for="transType">Type:</label>
               <select id="transType"name="type"required>
               <option value="Dépot">Dépot</option>
               <option value="Retrait">Retrait</option>
               </select>
           </div>
           <div class="form-group">
              <label for="transAmount">Montant:</label>
              <input type="number"
                     id="transAmount" name="amount" min="0.01"
                     step="0.01" required>
          </div>
          <button type="submit"
                  class="action=button succes-button">Exécuter la transaction</button>
          </form>
          <p id="transactionMesage"
             class="Message"></p>
      </section>
      <section id="create-account" class="page">
          <h2>Créer un Nouveau Compte Bancaire</h2>
          <form id="createAccountForm"
                class="standard-form">
        <div class="form-group">
          <label for="clientName">Nom du Client:</label>
          <input type="text"id="clientName"
                 required>
        </div>
            <div class="form-group">
             <label for="initialDeposit">Dépot initial:</label>
             <input type="number"
                    id="initialDeposit"min="0" step="0.01"
                    value="0.00" required>
            </div>
            <button type="submit"
                    class="action-button primary-button">Créer un compte</button>
            </form>
                <p id="createAcountMessage"
                   class="message"></p>
      </section>
      <section id="security" class="page">
       <h2>Gestion de la Securité</h2>
          <p>c'est ici que celui qui fait la sécurieté va implementer les formulaires pour la modification de mot de passe l'authentification à deux facteurs et la gestion des acces</p>
          <form id="passwordChangeForm"
                class="standard-form">
            <div class="form-group">
             <label for="oldPassmord">Ancien Mot de Passe:</label>
             <input type="password"
                    id="oldPassword" required>
            </div>
            <div class="form-group">
             <label for="newPwd">Nouveau Mot de Passe:</label>
             <input type="password" id="newPwd"
                    required>
             </div>
             <button type="submit"
                     class="action-button warning-button">Changer le mot de passe</button>
        </section>
       </form>
    </main>
    <script>
    document.addEventListtener('DOMcontentLoaded',() =>{
    document.querySelectorAll('.nav-link').forEach(link => {
           link.addEventListener('click',(e) =>{
                                 e.preventDefault();
        const targetld = 
              e.target.getAttribute('href').substring(1);
    document.querySelectorAll('.page').forEach(page => {
        page.classList.remove('active');
    });
    document.getElementaryByld(targetld);
               if (targetPage) {
                   targetPage.classList.add('active');
               }
    document.querySelectorAll('.nav-link').forEach(nav => nav.classList.remove('active'));
           });
              });
                 });
        const balanceElement =
              document.getElementByld('currentBalance');
              balanceElement.textContent = 1.595.00 FCFA;
             balanceElement.textContent = '#28a745';
        }
    </script>
</body>
